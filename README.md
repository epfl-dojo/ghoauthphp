This is a implementation of https://gist.github.com/aaronpk/3612742

## Setup
* `docker-compose build`
* `docker-compose up`
* run `composer install` in the www directory
* go to http://localhost:5000
